class SalonDetails{
  const SalonDetails(this.salonName,this.salonImagePath, this.salonLocation, this.distance, this.genderType, this.rating, this.fullAddress);
  final String salonName, salonImagePath, salonLocation, genderType, fullAddress, rating;
  // final List<String> details;
  final double distance;
}